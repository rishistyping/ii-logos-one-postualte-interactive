name = "op-postulate-chat"
main = "src/index.js"
compatibility_date = "2025-01-01"
workers_dev = true

# The OpenRouter API key is provided as a secret, NOT here.
# Run once after `wrangler login`:
#   wrangler secret put OPENROUTER_API_KEY
# (paste the key when prompted; never commit it.)
